name: Crown d3 QED computation
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
concurrency:
  group: crown-d3-screen-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  compute:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo superseded by crown-d3-deep.yml
